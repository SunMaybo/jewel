							Jewel自然语言描述
１．综述
	１．１　框架分四大模块，１．注册中心，２．配置管理，３．集群管理，４．服务提供，５．服务消费．
	１．２　过程调用
		１．２．１初始化加载

			通过注册中心，可以将服务结点注册到数据库，将服务的配置信心以及消费者信息注册到数据库．
			配置管理通过注册中心获取服务配置相关信息．
			集群管理通过注册中心获取服务结点
		
		１．２．２　数据同步
			
			有服务结点接入，注册中心触发事件同步服务结点与集群中结点
			有服务结点断开，注册中心触发事件同步服务结点与集群中结点
			有服务配置信息添加，注册中心触发事件将服务配置信息写入配置管理
			有服务配置信息添加，注册中心会将有关服务所在结点信息写入集群管理
２．过程描述

	２．１　服务端启动
		１．首先加载　app标签　标签的check设为false不过存在与否不影响启动
		２．加载registry标签，注册数据中心－－－连接zookeeper,check=false不管连接成功与否，为true必须连接，连接出错抛出运行异常
		３．加载协议标签，开启协议，如果协议register=true就是注册默认注册，此时条件１，条件２的check=true,必须连接成功．为false不做限制，当
			出现注册失败时－－异常，当出现app不存在时－－－－－异常．
		４．加载服务配置信息，register=false时－－－没有注册异常，为true－－－－注册失败异常．

	２．２　　客户端启动

		１．加载app标签可有可无
		２．加载注册标签，注册数据中心－－－连接zookeeper,check=false不管连接成功与否，为true必须连接，连接出错抛出运行异常
		３．加载消费者配置，通过注册中心查找对应的服务配置信息，存在缓存到配置管理，未发现的警告，并通过服务配置的app找到对应的结点并加入集群管理．
	
	２．３　注册中心与配置管理，集群管理数据强一致性

		２．３．１．服务配置信息的一致性
			１．当有服务接入时，注册中心触发事件将配置写入监听服务的客户端配置管理中．－－－－－显示服务已经添加
			２．服务变更时，注册中心触发事件将配置信息写入客户端的配置中．
		２．３．２．结点的一致性
			１．结点发生变化注册中心触发事件将结点变化和集群同步．
			２．服务接入，查询注册中心中结点是否存在对应zpp没有则将服务的结点加入集群，否在不做处理．


３．模块描述

	３．１．注册中心
		１．提供注册工厂选择存储方式
		２．注册提示注册信息
		３．提供注册中心接口，为配置中心提供写入，查询配置服务．
		４．为集群管理提供结点．
		５．事件触发，当注册中心的服务配置变更，结点变更时，触发事件．将数据和配合管理，集群管理的数据同步．

	３．２．　配置管理
		１．提供服务配置，工程配置，消费配置的本地缓存接口．
		２．提供来在注册中心服务变更的写入接口．
	３．３　集群管理
		１．提供来自注册中心的结点变更写入接口
		２．提供工厂方法生产负载集群，方式来自服务配置．－－－－１．轮循，２．加权轮循，３．随机算法，４．哈系一致．
		３．通过负载集群获取协议池．
		４．通过协议池发送数据到服务端．－－－－协议池用于通道选择，通道的优先级通过空闲，关闭等变化触发事件，来变更优先级．
	４．４　消费者
		１．通过代理工厂选择代理方式编码请求，发送数据
		２．所需配置信息通过配置管理获取
	５．５　提供者
		３．处理数据返回．
		４．配置信息来自配置管理．
					


